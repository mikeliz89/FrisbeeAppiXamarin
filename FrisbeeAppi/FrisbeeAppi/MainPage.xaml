<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FrisbeeAppi.MainPage" 
             Title="FrisbeeAppi">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Pelaajat"
                     Clicked="OnNavigateToPlayersListClicked" />
        <ToolbarItem Text="Radat"
                     Clicked="OnNavigateToTracksListClicked" />
        <ToolbarItem Text="+"
                     Clicked="OnGameAddedClicked" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout VerticalOptions="Center" HorizontalOptions="Center" Margin="0">
            <ListView x:Name="listView"
            Margin="20"
            ItemSelected="OnListViewItemSelected">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Text}"
                        Detail="{Binding StartDateTime}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>

</ContentPage>